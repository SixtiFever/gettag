{
  "name": "tag-landing-page",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tailwindcss -i css/input.css -o css/styles.css --watch",
    "build": "node scripts/build.mjs && tailwindcss -i css/input.css -o dist/css/styles.css --minify",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
